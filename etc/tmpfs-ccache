# tmpfs
TMPFS="/tmp/tmpfs-ccache"

# user tmpfs
USERTMPFS="$TMPFS/user"

# portage tmpfs
PORTAGETMPFS="$TMPFS/portage"

# persistent data
PERSISTENT="/var/cache/tmpfs-ccache"

# user ccache size. tmpfs size must be bigger than ccache size
USERSIZE=500M

# -a for archive; -H for preserve hard links; -x for don't cross filesystem
# boundaries
RSYNC_OPTS="-a -H -x"
